@model dynamic
<div class="d-flex align-items-center justify-content-between mb-2">
    <div class="d-flex gap-2 align-items-center">
        <input type="search" class="form-control form-control-sm" placeholder="Buscar..." aria-label="Buscar" />
        <button class="btn btn-sig btn-sm">Filtrar</button>
    </div>
    <div class="d-flex gap-2">
        <button class="btn btn-outline-light btn-sm">Exportar</button>
        <button class="btn btn-outline-light btn-sm">Columnas</button>
    </div>
</div>